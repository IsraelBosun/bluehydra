import ReferClient from './ReferClient';

export const metadata = {
  title: 'Become a referrer | Bluehydra Labs',
  description: 'Earn ₦10,000 for every business you refer to Bluehydra. Limited promo through July 2026.',
};

export default function ReferPage() {
  return <ReferClient />;
}
